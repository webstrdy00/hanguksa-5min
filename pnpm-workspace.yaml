packages:
  - frontend
  - backend

# 설치 시 빌드 스크립트 실행을 허용할 패키지 (pnpm 10 기본은 차단).
# - esbuild: vite 빌드에 필요한 네이티브 바이너리 설치
# - protobufjs: 앱인토스 CLI 의존성
# - cloudflared: `ait dev` 터널에 사용하는 공식 CLI 의존성
onlyBuiltDependencies:
  - cloudflared
  - esbuild
  - protobufjs
